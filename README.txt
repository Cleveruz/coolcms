Install

1) unpack downloaded archive
2) create database and its user on the server (in cPanel for e.g.)
3) copy or rename file config_sample.ini to config.ini and fill out the details
4) import coolcms.sql into created database
5) first user to be registered is admin

